= contacts

basic contacts redmine plugin adding contacts to project. Implemented with name only.

supporting: 
   custom fields
   activity stream
   attachements
   searching model
   permissons

TODO:
  redmine query to select contacts according to project. now used activeRecord query.
